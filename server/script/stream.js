/*
function fireStream() {
	xhr = $.ajax({
		type: "GET",
		url: 'http://localhost:9292/updates'
	});

	console.log(xhr);

	pollTimer = setInterval(pollLatestResponse, 500);

}

function pollLatestResponse() {
    var allMessages = xhr.responseText;
	console.log(allMessages);
}
*/